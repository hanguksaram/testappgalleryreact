import React from 'react';
import ImageUploader from '../popups/imageUploader'

const PopupHandler = () => {
    return (
        <div>
            <ImageUploader/>
        </div>
    );
};

export default PopupHandler 